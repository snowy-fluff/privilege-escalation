#!/bin/bash

rm /challenge/.init
find -L /challenge -type f -exec chmod 0444 {} +

mkdir -p /challenge/bin
ln -sf /usr/sbin/start-stop-daemon /challenge/bin/start-stop-daemon

chmod u+s /usr/sbin/start-stop-daemon
