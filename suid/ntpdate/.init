#!/bin/bash

rm /challenge/.init
find -L /challenge -type f -exec chmod 0444 {} +

mkdir -p /challenge/bin
ln -sf /usr/sbin/ntpdate /challenge/bin/ntpdate
ln -sf /usr/bin/ntpdig /challenge/bin/ntpdig

chmod u+s /usr/sbin/ntpdate
chmod u+s /usr/bin/ntpdig
